# Supabase project config (used by CLI for local dev and deploy)
# See: https://supabase.com/docs/guides/functions/function-configuration

[functions.parse-receipt]
# Skip gateway JWT verification so the request reaches the function.
# The function validates the caller and ensures storage_path belongs to them.
verify_jwt = false

[functions.ask-receipts]
verify_jwt = false
